<h1>All Tattoos(<%= @tattoos.count %>)</h1>
<div class="container">
    <% @tattoos.each do |tattoo| %>
      <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
        <div class="tattoos-card">
          <div class="tattoos-card-picture" style="background-image: url('<%= cl_image_path tattoo.photo%>')">
            <%= link_to "", tattoo_path(tattoo), class: "tattoos-card-link" %>
          </div>
          <div class="tattoos-card-header">
            <h3><%=tattoo.name %></h3>
            <p><%=tattoo.created_at.strftime("%m/%d/%Y")%></p>
          </div>
          <div class="tattoos-card-footer">
            <div class="tattoos-card-footer-avatar" style="background-image: url('<%= image_path tattoo.user.picture %>')">
            </div>
          <%=tattoo.user.first_name.capitalize + " " + tattoo.user.last_name.upcase %>
          </div>
        </div>
      </div>
    <% end %>
</div>
